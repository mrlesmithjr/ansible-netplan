---
- hosts: localhost
  connection: local
  pre_tasks:
    - name: Installing Pre-Reqs
      command: sudo apt-get update && apt-get -y install apt-utils
      when: ansible_os_family == "Debian"
  roles:
    - ansible-netplan
